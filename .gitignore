# Rust
/target
# Web build output (regenerated by `vite build`)
/web/dist
# Node
node_modules
# Editor / OS
.DS_Store

# Claude Code machine-local settings
.claude/settings.local.json

references/
tmp/
